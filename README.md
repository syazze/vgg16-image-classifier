# VGG16 Image Classifier

A deep learning image classification web app using VGG16 fine-tuned model deployed with Flask.

## Installation

1. Clone repository
2. Install dependencies: `pip install -r requirements.txt`
3. Run: `python app.py`
4. Open: `http://localhost:5000`